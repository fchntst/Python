# when we want to organize the code in a specific sector, then
# we need to import something from the sector we want to work with.

import math # <-- like this
            # if we now type math. then we will recieve many tools for math
x = 3.7 
conversion01 = round(x)
print(conversion01)

y = 2.3
conversion02 = round(y)
print(conversion02)